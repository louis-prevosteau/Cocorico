import { Paper } from '@mui/material';
import { User } from 'models';
import React from 'react';

const DashboardActions = ({ profile }: { profile: User }) => {
    return (
        <Paper elevation={3}>
            
        </Paper>
    );
};

export default DashboardActions;